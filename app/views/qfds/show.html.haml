%h1 QFD: #{h(@qfd.name)}


%h2 Houses of Quality
= link_to("Create new HOQ", new_qfd_hoq_path(@qfd))

%ol
  = list_of(@qfd.hoqs) do |hoq|
    = link_to(h(hoq.name), [@qfd, hoq])

= link_to("Download this QFD", download_qfd_path(@qfd))
%br
= link_to("My QFDs", qfds_path)
%br
- if @qfd.user == current_user
  = link_to("Invite a collaborator", new_invitation_path(:invitation => {:qfd_id => @qfd.id}))
